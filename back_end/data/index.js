const recipeData = require("./recipes");
const userData = require("./users")
const reviewData = require("./reviews");

module.exports = {
  recipes: recipeData,
  users: userData,
  reviews: reviewData
};
